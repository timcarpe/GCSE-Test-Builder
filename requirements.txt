# Core dependencies (from pyproject.toml)
Pillow
numpy
PyMuPDF

# GUI framework
PySide6>=6.0.0
qtawesome

# PDF generation
reportlab

# Data serialization and config
pyyaml
joblib

# File locking for concurrent access
portalocker

# macOS specific
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'
